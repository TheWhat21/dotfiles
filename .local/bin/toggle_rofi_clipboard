#!/usr/bin/env bash

pgrep rofi >/dev/null 2>&1 && killall rofi || cliphist list | rofi -dmenu -display-columns 2 -theme-str "window { location: southwest; anchor: southwest; width: 25%;}" | cliphist decode | wl-copy
